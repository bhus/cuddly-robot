{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "java -jar app.jar --host 0.0.0.0 --port ${PORT:-8080}",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}